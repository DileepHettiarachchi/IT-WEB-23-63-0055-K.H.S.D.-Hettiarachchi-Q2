@page
@model IT_WEB_23_63_0055_K.H.S.D.Hettiarachchi_Q2.Pages.Courses_Info.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Course_Info[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course_Info[0].Lecturer_Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Course_Info) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lecturer_Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Course_Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Course_Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Course_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
